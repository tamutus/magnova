<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<link rel="stylesheet" type="text/css" href="/stylesheets/magnova.css">
	<link rel="stylesheet" type="text/css" href="/stylesheets/talk.css">
	<link rel="preconnect" href="https://fonts.gstatic.com">
	<link href="https://fonts.googleapis.com/css2?family=Montserrat&family=Poppins:wght@500&display=swap" rel="stylesheet">
	<base href="/">
	<title><%= title %></title>
</head>
<body>
<%- include("../partials/navbar") %>
<div id="allContent" class="expanded">
<h1>Fora</h1>
<h2>All public comments</h2>
<% for(comment of comments){ %>
    <% let thisComment = comment; thisComment.text = thisComment.text.replace().replace( /(<([^>]+)>)/ig, ' '); %>
    <div class="comment">
        <a href="<%= `/talk/comment/${thisComment._id}` %>"><button type="button">Context</button></a>
        <div class="comment-text">
            <% if(thisComment.text.length <= 300) { %>
                <%- thisComment.text %>
            <% } else { %>
                <%- thisComment.text.slice(0, 298) + "..." %>
            <% } %>
        </div>
    </div>
<% } %>

</div>
<script src="https://d3js.org/d3.v4.js"></script>
<script src="scripts/nav.js"></script>

</body></html>